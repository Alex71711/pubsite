## 🔧 Режим технических работ

### Описание
Функция позволяет легко включать/выключать режим "Технические работы", который показывает заглушку вместо основного содержимого сайта.

### Как использовать

#### 1. **Включить режим из админки**
- Перейдите в **Администрация → Настройки**
- Найдите раздел **"Технические работы"**
- Нажмите на переключатель **"Режим обслуживания"**
- Отредактируйте сообщение, которое будут видеть пользователи (опционально)
- При включении режима:
  - Все пользователи увидят красивую страницу с информацией о техработах
  - Администраторы смогут продолжить работу в админке (`/admin/*`)
  - Статические файлы остаются доступны

#### 2. **Программный API**
```bash
curl -X POST http://localhost:5000/api/admin/maintenance/toggle \
  -H "Content-Type: application/json" \
  -d '{
    "enabled": true,
    "message": "Кастомное сообщение о техработах"
  }'
```

### Технические детали

#### Файлы, которые были изменены:
1. **`data/site.json`** - добавлено поле `maintenance` с флагом и сообщением
2. **`app.py`** - добавлена логика проверки режима в `@before_request`
3. **`templates/maintenance.html`** - создана страница заглушки
4. **`templates/admin_settings.html`** - добавлен переключатель в админке

#### Как это работает:
- Когда `maintenance.enabled = true`, функция `_maintenance_check_before_request()` перенаправляет всех пользователей (кроме админов) на `/maintenance`
- Админская панель (`/admin/*`) остается полностью доступна
- Статические файлы (`/static/*`) не блокируются
- Сообщение о техработах кастомизируется через админку или API

#### Маршруты:
- `GET /maintenance` - страница с информацией о техработах
- `POST /api/admin/maintenance/toggle` - переключение режима (только для админов)

### Примеры

**Включить режим:**
```json
{
  "enabled": true,
  "message": "Сейчас ведутся работы по обновлению. Вернёмся через 1 час."
}
```

**Выключить режим:**
```json
{
  "enabled": false
}
```

### Страница заглушки
На странице `/maintenance` пользователи видят:
- ✅ Красивый дизайн в стиле сайта
- ✅ Кастомное сообщение о техработах
- ✅ Контактную информацию (телефон, email)
- ✅ Ссылки на соцсети (если заполнены)
- ✅ Декоративные элементы (волны, иконка)
